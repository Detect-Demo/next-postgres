# next-postgres

Detect owned demo for **Next.js + Postgres**.

Open in Detect (replace host when public origin is live):

```markdown
[![Open in Detect](https://<DETECT_HOST>/badge.svg)](https://<DETECT_HOST>/open/github/detect-demo/next-postgres)
```

Stack signals: `next` in package.json, Prisma `postgresql` datasource, `DATABASE_URL` in `.env.example`.
